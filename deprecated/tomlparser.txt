// func setConfigurationStringValue(config *Configuration, fieldToSet string, value string) {
// 	reflectionValue := reflect.Indirect(reflect.ValueOf(config))

// 	for i := 0; i < reflectionValue.NumField(); i++ {
// 		field := reflectionValue.Type().Field(i).Name
// 		if field == fieldToSet {
// 			reflectionValue.Type().Field(i).
// 		}
// 	}
// }

// collectAcceptedParameters collects the set of configuration parameters used
// in the Configuration type using reflection.
// func collectAcceptedParameters(config Configuration) []string {
// 	var configParams []string

// 	reflectionValue := reflect.Indirect(reflect.ValueOf(config))

// 	for i := 0; i < reflectionValue.NumField(); i++ {
// 		configParams = append(configParams, reflectionValue.Type().Field(i).Name)
// 	}

// 	return configParams
// }